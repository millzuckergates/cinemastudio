<form method="post" action="{{ path('app_realisateurs_delete', {'id': realisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ realisateur.id) }}">
    <button class="btn">Delete</button>
</form>
